
if (typeof(MIDI) === "undefined") MIDI = {};

MIDI.ChordCharter = {
  init: function(){
    this.song = {
      title: 'None',
      format: 'chord',
      notes: [],
    };
    this.currentNotes = [];
  },
  insertNotes: function() {
    
  },
}